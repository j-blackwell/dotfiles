# OpenCode Custom Instructions

- When dealing with Python repositories, always favor using 'uv' for dependency management and script execution.
- Adhere to the established architectural patterns of the project (e.g., check `setup/arch/` for dotfiles structure).
- Favor utilizing local utilities in `~/.local/bin/` where appropriate.
- Always check for existing scripts or configuration before creating new ones.
