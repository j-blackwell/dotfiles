# Enable VA-API hardware video encode (AMD Renoir/radeonsi via Mesa).
# Chrome disables this by default on Linux; without it, webcam/screen-share
# encoding in Meet/Slack calls runs in software and drives the fan hard.
--enable-features=VaapiVideoEncoder,VaapiIgnoreDriverChecks
